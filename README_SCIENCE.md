# Science Notes / Data Pipeline

- Automatische, anonymisierte Session-Erfassung bei `stopExp()`
- Eine anonyme UID pro Browser (localStorage)
- Speicherung in Vercel Blob (eine Datei pro Session)
- Export optional via `/api/exp/export`
- Keine PII, keine Cookies, keine externen Tracker
